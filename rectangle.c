#include<stdio.h>
void main()
{
	int length ,breath ,area;
	scanf("%d%d",&length,&breath);
	area=length*breath;
	printf(" the area of the rectangle when %d and %d is given is %d",length,breath,area);
}
